import React from 'react';

const OrderSummary = ({ orderDetails }) => {
  return (
    <div>
      <h2>Order Summary</h2>
      {/* Display order details, such as products, total price, etc. */}
    </div>
  );
};

export default OrderSummary;
